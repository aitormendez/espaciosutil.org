# Staging dedicado de Espacio Sutil

Fecha: 2026-09-05. Aprovisionamiento, despliegue, acceso administrativo e aislamiento inicial completados y verificados.

## Identidad y alcance

- Droplet: `espacio-sutil-staging`, `138.68.135.185`, Londres, Ubuntu 24.04.4 LTS, 1 vCPU, 2 GiB RAM y 50 GiB SSD.
- Dominio: `stage.espaciosutil.org`; DNS autoritativo en DigitalOcean. Registro A cambiado desde `167.99.95.143` y comprobado directamente contra `ns1.digitalocean.com`.
- Producción conserva `134.209.21.17`. No se ha desplegado ni escrito en ella.
- Inventario y alias WP-CLI actualizados. El servidor antiguo está retirado; no debe usarse para ninguna operación.
- Rama inicial: `codex/espacio-sutil-staging`. Las credenciales del nuevo staging se guardan exclusivamente en el vault cifrado de su entorno.

## Criterios de aceptación

SSH por clave, acceso administrativo probado antes de cerrar root, firewall, servicios con inicio automático, HTTPS válido, WordPress con base de datos propia y cuenta sintética, aislamiento de correo e integraciones, y pruebas de que el aislamiento sólo se aplica a staging. No se copian alumnos, pedidos, suscripciones ni claves de integraciones reales.

## Aislamiento inicial

El entorno desactiva cron de WordPress y el cron de Trellis, Matomo y las credenciales SMTP/Bunny/YouTube heredadas. El correo del sistema apunta a un puerto local sin servidor. Un mu-plugin devuelve fallo en `wp_mail`, bloquea toda petición de la API HTTP de WordPress antes del transporte y desactiva la sincronización Listmonk y la consulta automática de pedidos Stripe. También bloquea actualizaciones y llamadas de comprobación de WordPress; las dependencias se instalan mediante el despliegue.

Este bloqueo cubre las APIs de WordPress; no es un firewall de salida para código que use cURL o sockets directamente. No instalar claves reales ni importar opciones de producción. La futura prueba de Bunny necesitará una excepción explícita y restringida, con pruebas de autorización. El acceso público al host no equivale a autorización sobre las lecciones.

## Compatibilidad y capacidad

MariaDB usa el paquete de Ubuntu en staging, conservando por defecto el repositorio externo para los demás entornos. PHP 8.4 tiene un máximo de tres procesos FPM y 256 MiB por proceso; se configura 1 GiB de swap. Los assets se compilan en el Mac. No es una prueba de carga.

Se corrigen los plugins de Trellis para Python 3.12/Ansible 2.18 y el hook de bloques: instalación y compilación son comandos separados y los assets se copian al release. El workflow manual de staging exige una clave de host conocida; no acepta ciegamente el resultado de `ssh-keyscan`.

## Validación registrada

- DNS autoritativo: staging `138.68.135.185`, raíz `134.209.21.17`.
- Pruebas PHP de aislamiento: staging, production y development correctas; seguridad del hook de Composer correcta.
- Aprovisionamiento: `ok=156`, `changed=102`, `failed=0`, `unreachable=0`.
- Primer despliegue: `1adaec0`, release `20260905151608`; `ok=53`, `failed=0`. Tema y bloques compilados en el Mac y copiados al servidor; no se versionan las diferencias generadas del build.
- Infraestructura: `staging-verify.yml`, 15 comprobaciones/tareas correctas, cero cambios y cero fallos. Nginx, PHP-FPM, MariaDB, ferm y fail2ban activos y habilitados. SSH root y contraseña desactivados. MariaDB `127.0.0.1:3306`, memcached `127.0.0.1:11211`; sin receptor SMTP local ni cron de WordPress.
- HTTPS, portada, noindex e inicio de sesión administrativo correctos mediante cliente HTTP con validación TLS. La sesión de prueba se destruyó al terminar.
- WordPress `7.0.3`, ACF Pro `6.8.5`, Paid Memberships Pro `3.6.5`, PHP `8.4.25`. Una única cuenta sintética; no se importó ninguna base de datos de producción.
- `wp_mail` devuelve fallo y las peticiones HTTP a Stripe, Mailgun y webhook sintético devuelven `staging_outbound_disabled`. Listmonk y cron desactivados. Credenciales temporales de Composer eliminadas.
- El navegador mostró la portada con Sage; el contenido es el de una instalación nueva. Los enlaces editoriales y las lecciones aún requieren fixtures o una migración selectiva posterior.

## Operación y reversión

Usar Trellis desde la rama que contiene el inventario nuevo y limitar siempre el primer aprovisionamiento a `138.68.135.185`. El archivo de claves de host usado inicialmente es `/private/tmp/espacio-sutil-staging-known-hosts`; conservar su entrada verificada en el gestor habitual y en el secreto GitHub del entorno staging antes de automatizar despliegues. No restaurar el DNS a la IP retirada como rollback. Un release fallido se revierte al anterior del mismo staging; si aún no existe, mantener el sitio fuera de servicio mientras se corrige.

Los backups diarios están seleccionados en DigitalOcean. Su existencia y una restauración de prueba son verificaciones distintas del aprovisionamiento.

## Distribución del código y próximos despliegues

La revisión automática rechazó subir esta rama a GitHub porque el repositorio es público y el cambio incluye un vault cifrado y configuración de infraestructura. No se publicó la rama ni se modificó `main`. Los commits permanecen en el repositorio local; un `git bundle` se transfirió directamente al servidor autorizado y se importó en `/home/web/staging-source.git`, con permisos privados.

El origen privado también queda configurado en las variables de staging. El despliegue usado, ejecutado desde `trellis/` con su entorno Python, fue `ansible-playbook deploy.yml -e env=staging -e site=espaciosutil.org -e project_git_repo=/home/web/staging-source.git --limit 138.68.135.185`. Antes de repetirlo, actualizar ese repositorio del VPS desde la rama local mediante un bundle; no asumir que el origen público contiene la rama. Mantener la comprobación de host SSH. La publicación en GitHub y el workflow remoto quedan pendientes de decidir cómo distribuir la configuración privada; no impiden desplegar directamente desde esta máquina.

## Pendientes de producto

Preparar lecciones y cuentas de prueba, corregir la exposición de metadatos premium detectada en la auditoría móvil, cerrar el contrato de sesión/progreso y validar reproducción Bunny. Este staging básico no acredita todavía la prueba vertical móvil ni reproduce todo el contenido editorial. No hay pagos reales ni configuración Stripe copiada. El registro DNS heredado `matomo.stage.espaciosutil.org` no se modificó; sigue apuntando a la IP retirada y no debe usarse. No se ha probado la restauración de un backup.

Se verificó una segunda ejecución del rol MariaDB: `ok=14`, `changed=0`, `failed=0`. La autenticación admite tanto el primer arranque como el mantenimiento posterior con la contraseña cifrada. La optimización de plantillas exigió activar `html-forms`; queda incluido en la instalación inicial.

## Resultado final

Release activo `20260905152352`, código web `a6fc6ea5`, desplegado con Trellis: `ok=57`, `changed=26`, `failed=0`, `unreachable=0`. La repetición del despliegue sobre WordPress instalado compiló todas las plantillas y conservó la única cuenta sintética. Las comprobaciones HTTP y de WordPress se repitieron sobre ese release y pasaron, incluida la ausencia de URL de Matomo en la configuración del navegador. Véase `staging-verificacion-2026-09-05.json` para el resumen sanitizado.
