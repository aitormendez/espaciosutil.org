# Staging dedicado de Espacio Sutil

Fecha: 2026-09-05. Aprovisionamiento y copia de producción completados y verificados. La instalación inicial que se describe a continuación fue sustituida por la copia documentada al final.

## Identidad y alcance

- Droplet: `espacio-sutil-staging`, `138.68.135.185`, Londres, Ubuntu 24.04.4 LTS, 1 vCPU, 2 GiB RAM y 50 GiB SSD.
- Dominio: `stage.espaciosutil.org`; DNS autoritativo en DigitalOcean. Registro A cambiado desde `167.99.95.143` y comprobado directamente contra `ns1.digitalocean.com`.
- Producción conserva `134.209.21.17`. No se ha desplegado ni escrito en ella.
- Inventario y alias WP-CLI actualizados. El servidor antiguo está retirado; no debe usarse para ninguna operación.
- Rama inicial: `codex/espacio-sutil-staging`. Las credenciales del nuevo staging se guardan exclusivamente en el vault cifrado de su entorno.

## Criterios de aceptación

SSH por clave, acceso administrativo probado antes de cerrar root, firewall, servicios con inicio automático, HTTPS válido, WordPress con base de datos propia y cuenta sintética, aislamiento de correo e integraciones, y pruebas de que el aislamiento sólo se aplica a staging. La instalación inicial no incluía datos reales; posteriormente se autorizó su copia conservando usuarios e historial, con las restricciones descritas al final.

## Aislamiento inicial

El entorno desactiva cron de WordPress y el cron de Trellis, Matomo y las credenciales SMTP/Bunny/YouTube heredadas. El correo del sistema apunta a un puerto local sin servidor. Un mu-plugin devuelve fallo en `wp_mail`, bloquea toda petición de la API HTTP de WordPress antes del transporte y desactiva la sincronización Listmonk y la consulta automática de pedidos Stripe. También bloquea actualizaciones y llamadas de comprobación de WordPress; las dependencias se instalan mediante el despliegue.

Este bloqueo cubre las APIs de WordPress; no es un firewall de salida para código que use cURL o sockets directamente. No instalar claves reales; las opciones importadas se depuran antes de abrir el acceso web. La futura prueba de Bunny necesitará una excepción explícita y restringida, con pruebas de autorización. El acceso público al host no equivale a autorización sobre las lecciones.

## Compatibilidad y capacidad

MariaDB usa el paquete de Ubuntu en staging, conservando por defecto el repositorio externo para los demás entornos. PHP 8.4 tiene un máximo de tres procesos FPM y 256 MiB por proceso; se configura 1 GiB de swap. Los assets se compilan en el Mac. No es una prueba de carga.

Se corrigen los plugins de Trellis para Python 3.12/Ansible 2.18 y el hook de bloques: instalación y compilación son comandos separados y los assets se copian al release. El workflow manual de staging exige una clave de host conocida; no acepta ciegamente el resultado de `ssh-keyscan`.

## Validación registrada

- DNS autoritativo: staging `138.68.135.185`, raíz `134.209.21.17`.
- Pruebas PHP de aislamiento: staging, production y development correctas; seguridad del hook de Composer correcta.
- Aprovisionamiento: `ok=156`, `changed=102`, `failed=0`, `unreachable=0`.
- Primer despliegue: `1adaec0`, release `20260905151608`; `ok=53`, `failed=0`. Tema y bloques compilados en el Mac y copiados al servidor; no se versionan las diferencias generadas del build.
- Infraestructura: `staging-verify.yml`, 15 comprobaciones/tareas correctas, cero cambios y cero fallos. Nginx, PHP-FPM, MariaDB, ferm y fail2ban activos y habilitados. SSH root y contraseña desactivados. MariaDB `127.0.0.1:3306`, memcached `127.0.0.1:11211`; sin receptor SMTP local ni cron de WordPress.
- HTTPS, portada, noindex e inicio de sesión administrativo correctos mediante cliente HTTP con validación TLS. La sesión de prueba se destruyó al terminar.
- WordPress `7.0.3`, ACF Pro `6.8.5`, Paid Memberships Pro `3.6.5`, PHP `8.4.25`. Una única cuenta sintética; no se importó ninguna base de datos de producción.
- `wp_mail` devuelve fallo y las peticiones HTTP a Stripe, Mailgun y webhook sintético devuelven `staging_outbound_disabled`. Listmonk y cron desactivados. Credenciales temporales de Composer eliminadas.
- El navegador mostró la portada con Sage; el contenido es el de una instalación nueva. Los enlaces editoriales y las lecciones aún requieren fixtures o una migración selectiva posterior.

## Operación y reversión

Usar Trellis desde la rama que contiene el inventario nuevo y limitar siempre el primer aprovisionamiento a `138.68.135.185`. El archivo de claves de host usado inicialmente es `/private/tmp/espacio-sutil-staging-known-hosts`; conservar su entrada verificada en el gestor habitual y en el secreto GitHub del entorno staging antes de automatizar despliegues. No restaurar el DNS a la IP retirada como rollback. Un release fallido se revierte al anterior del mismo staging; si aún no existe, mantener el sitio fuera de servicio mientras se corrige.

Los backups diarios están seleccionados en DigitalOcean. Su existencia y una restauración de prueba son verificaciones distintas del aprovisionamiento.

## Distribución del código y próximos despliegues

Los seis commits iniciales hasta `f8933a76dafd12263d3d02150d00fa530922ddcd` se publicaron en `codex/espacio-sutil-staging` tras la autorización explícita del usuario. Los vaults estaban cifrados y se verificó la ausencia de sus secretos conocidos en texto claro. `main` no se modificó. La copia posterior y su configuración se versionan localmente; no se han publicado con esta operación.

El despliegue conserva el origen privado `/home/web/staging-source.git`. Antes de repetir `ansible-playbook deploy.yml -e env=staging -e site=espaciosutil.org -e project_git_repo=/home/web/staging-source.git --limit 138.68.135.185`, actualizar ese repositorio del VPS desde la rama local mediante un bundle y mantener la comprobación de host SSH. No asumir que el origen público contiene los cambios posteriores.

## Pendientes de producto

Con las lecciones y cuentas de prueba ya disponibles, corregir la exposición de metadatos premium detectada en la auditoría móvil, cerrar el contrato de sesión/progreso y validar reproducción Bunny. La copia de contenido no acredita todavía la prueba vertical móvil. No hay pagos reales ni configuración Stripe copiada. El registro DNS heredado `matomo.stage.espaciosutil.org` no se modificó; sigue apuntando a la IP retirada y no debe usarse. No se ha probado la restauración de un backup.

Se verificó una segunda ejecución del rol MariaDB: `ok=14`, `changed=0`, `failed=0`. La autenticación admite tanto el primer arranque como el mantenimiento posterior con la contraseña cifrada. La optimización de plantillas exigió activar `html-forms`; queda incluido en la instalación inicial.

## Resultado final

Release activo `20260905152352`, código web `a6fc6ea5`, desplegado con Trellis: `ok=57`, `changed=26`, `failed=0`, `unreachable=0`. La repetición del despliegue sobre WordPress instalado compiló todas las plantillas y conservó la única cuenta sintética. Las comprobaciones HTTP y de WordPress se repitieron sobre ese release y pasaron, incluida la ausencia de URL de Matomo en la configuración del navegador. Véase `staging-verificacion-2026-09-05.json` para el resumen sanitizado.

## Copia de producción autorizada

El usuario autorizó importar la base de datos completa y conservar los datos reales para un entorno temporal. Se conservan nombres, correos, hashes de contraseña e historial; no se aplicó anonimización general. Antes de importar se restringió Nginx a la IP pública actual del usuario, guardada en el vault cifrado como `vault_staging_allowed_ips`. No basta con que el dominio sea de pruebas para hacerlo privado. Un dispositivo conectado a otra red recibirá 403; si cambia la IP autorizada, actualizar el vault y ejecutar `staging-access.yml`. La excepción pública de ACME permite renovar HTTPS.

Criterios de aceptación: origen y destino comprobados, respaldo previo, contenido y medios íntegros, cuatro estados de membresía sintéticos correctos, credenciales de integración y sesiones retiradas, acceso administrativo válido, correo y automatismos bloqueados y rechazo de una conexión externa. Ante un fallo de importación o depuración se conserva `/srv/www/espaciosutil.org/shared/staging-import.lock` para responder 503 hasta corregir o restaurar.

Se exportó mediante una transacción de lectura de MariaDB y se transmitió directamente por SSH a staging, sin volcado en el Mac ni archivo en producción. Antes de cargar el WordPress importado se ejecutó `ops/staging/sanitize.sql`: retirada de opciones de integración, sesiones, contraseñas de aplicación, claves de activación, tokens de pasarela, webhooks y colas históricas. Se mantuvo el `.env` propio de staging, se sustituyeron las URL con WP-CLI respetando datos serializados y se recompilaron las plantillas. El volcado temporal de producción se eliminó después de verificar la copia.

Resultado comprobado: 92 lecciones CDE publicadas, una futura, 77 páginas publicadas y 253 adjuntos. Se copiaron los 16 usuarios de origen y se añadieron `staging-admin` y cuatro suscriptores sintéticos: `cde-test-active`, `cde-test-none`, `cde-test-expired` y `cde-test-cancelled`. Total: 21 usuarios. Sólo el estado activo obtiene membresía CDE según PMPro. La contraseña de pruebas está en `vault_staging_test_password`, dentro del vault cifrado; no se incluye en scripts ni logs.

Los 1.391 archivos de uploads de producción están presentes con el mismo SHA-256 individual. Staging contiene además un `.htaccess` previo; no hubo archivos de origen ausentes ni diferentes. El acceso HTTPS y login administrativo pasaron; se destruyó la sesión sintética después. La conexión autorizada obtiene 200 y una petición desde el host de producción recibe 403. El registro A autoritativo sigue apuntando a `138.68.135.185`.

PMPro regenera cinco tareas periódicas pendientes al arrancar. Las colas históricas fueron vaciadas, pero no se pretende mantener vacía una cola que el plugin vuelve a crear: se bloquean tanto cron como el runner asíncrono de Action Scheduler. Las comprobaciones de membresía, ausencia de sesiones y opciones de integración, sandbox y automatismos están en `ops/staging/verify-copy.php`. Las pruebas de aislamiento se repitieron en staging y verificaron correo, HTTP, Listmonk y Matomo bloqueados. Las pruebas unitarias también comprobaron que producción no registra estos bloqueos.

El release sigue siendo `20260905152352`, originalmente basado en `a6fc6ea5`. En esta operación se aplicaron directamente el mu-plugin de aislamiento y su prueba actualizados; el commit del release por sí solo no identifica ese parche. Sus huellas se registran en el JSON de evidencia. Actualizar el origen privado antes de otro despliegue para conservar el cambio.

### Respaldo y reversión

El respaldo del staging previo está en `/home/web/staging-import/before.sql`, dentro de un directorio privado. SHA-256: `0ba50354741b1d83ba34d332e10127668dcc7348d832f244b06b3aab054ad5f7`. Antes de restaurarlo, comprobar hostname `espacio-sutil-staging` y base `espaciosutil_org_staging`, activar el archivo de mantenimiento, ejecutar `wp db import /home/web/staging-import/before.sql` desde el release de staging, limpiar cachés y verificar el login y aislamiento antes de retirar el bloqueo. Los medios adicionales pueden conservarse: la importación no borró los previos. No se ha ensayado esta restauración ni la del backup de DigitalOcean.

El siguiente incremento es corregir la autorización premium de las rutas existentes y cerrar la sesión/API móvil antes de permitir la reproducción Bunny. El bloqueo HTTP actual impide consultar Bunny; los fallos de seguridad de la auditoría siguen pendientes y la restricción de IP no sustituye su corrección.

## Estado posterior: API y cliente móvil

Los pendientes anteriores corresponden a la preparación de la copia. La corrección de acceso, excepción Bunny y pruebas de la API están documentadas en [la ejecución móvil](cde-mobile-autorizacion-2026-09-05.md), con sus recibos y hashes. Producción permanece fuera de esta operación.
